<!-- Rendered at https://devops-thiago.github.io/ThrillhouseBot/commands/.
     Content is included from the README between the mkdocs:commands markers,
     so the README stays the single source of truth. -->

# Commands

{%
  include-markdown "../README.md"
  start="<!-- mkdocs:commands:start -->"
  end="<!-- mkdocs:commands:end -->"
%}
